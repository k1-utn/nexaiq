{
  "name": "nexaiq",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.16.0",
  "workspaces": [
    "apps/web",
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @nexaiq/web",
    "dev:mobile": "npm run start --workspace @nexaiq/mobile",
    "build": "npm run build --workspace @nexaiq/shared && npm run build --workspace @nexaiq/web",
    "lint": "npm run lint --workspace @nexaiq/web",
    "typecheck": "npm run typecheck --workspace @nexaiq/shared && npm run typecheck --workspace @nexaiq/web && npm run typecheck --workspace @nexaiq/mobile",
    "test": "npm run test --workspace @nexaiq/shared",
    "db:test:linked": "supabase db query --linked --file supabase/tests/tenant_isolation.sql && supabase db query --linked --file supabase/tests/rls_cross_tenant.sql",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "supabase": "2.116.0"
  },
  "dependencies": {
    "@supabase/ssr": "0.12.6",
    "@supabase/supabase-js": "2.115.0"
  }
}
